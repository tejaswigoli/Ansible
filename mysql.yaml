- name: configure mysql server
  hosts: backend
  become: yes
  tasks:
  - name: Install MYSQL server
    dnf:
     name: mysql-server
     state: installed
    
  - name: Start and enable mysql service
    service:
     name: mysqld
     state: started
     enabled: yes